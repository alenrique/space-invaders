<Page
    x:Class="Space_Invaders.GamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Space_Invaders"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    IsTabStop="True">

    <Grid>
        <Canvas x:Name="GameCanvas" Width="1000" Height="600" Background="Black" KeyDown="HandleKeyboardInput">

            <TextBlock x:Name="ScoreText"
                       Text="{Binding ScoreText, FallbackValue='Score: 0'}"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="White"
                       Canvas.Left="10"
                       Canvas.Top="10"/>

            <TextBlock x:Name="LifesText"
                       Text="{Binding LivesText, FallbackValue='Lives: 3'}"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="White"
                       Canvas.Left="900"
                       Canvas.Top="10"/>
            
            <TextBlock x:Name="GameOverText"
                       Text="GAME OVER"
                       FontSize="80"
                       FontWeight="Bold"
                       Foreground="Red"
                       Visibility="Collapsed" 
                       Canvas.Left="150"
                       Canvas.Top="250"/>
            
            </Canvas>
        
    </Grid>
</Page>